[project]
name = "emulator-workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fasta2a>=0.5.0",
    "litellm>=1.74.6",
    "pyyaml>=6.0.2",
    "typer>=0.16.0",
    "uvicorn>=0.30.0",
]


[tool.uv.workspace]
members = [
    "pgadapter-a2a"
]

[dependency-groups]
dev = [
    "docker>=7.1.0",
    "pytest>=8.4.1",
    "ruff>=0.12.4",
]
