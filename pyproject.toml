[project]
name = "emulator-workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fasta2a>=0.5.0",
    "typer>=0.16.0",
]


[tool.uv.workspace]
members = [
    "pgadapter-a2a"
]
